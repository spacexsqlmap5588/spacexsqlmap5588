gg.alert("SevenGamingID X-Play", "Menu Hack")
gg.toast("Subscribe")
while true do
  if gg.isVisible(true) then
     menuk = 1
     gg.setVisible(false)
   end
   START = 1
   function START()
   menu = gg.choice({
   	"Percobaan",
       "Isi Sendiri",
       "Isi Sendiri",
       "⚠️⛔KELUAR⛔⚠️"
       }, nil, "🇮🇩SevenGamingID X-Play🇮🇩")
       if menu == 1 then
          ch1()
        end
       if menu == 2 then
          ch2()
        end
       if menu == 3 then
           ch3()
        end
       if menu == 4 then
           os.exit()
        end
       if menu == nil then
       else
       end
       menuk = -1
     end
     
     function ch1()
     gg.searchNumber("18000", gg.TYPE_DWORD)
     gg.getResults(100)
     gg.editAll("-99999", gg.TYPE_DWORD)
     gg.clearResults()
     gg.toast("Done")
     end
     
     function ch2()
     gg.searchNumber("MASUKAN ANGKA TARGET", gg.TYPE_DWORD)
     gg.getResults(100)
     gg.editAll("MASUKAN ANGKA YANG MAU DI EDIT", gg.TYPE_DWORD)
     gg.clearResults()
     gg.toast("Done")
     end
     
     function ch3()
     gg.searchNumber("MASUKAN ANGKA TARGET", gg.TYPE_DWORD)
     gg.getResults(100)
     gg.editAll("MASUKAN ANGKA YANG MAU DI EDIT", gg.TYPE_DWORD)
     gg.clearResults()
     gg.toast("Done")
     end
     
     
    print("Subscribe Sekarang")
    if menuk == 1 then
       START()
     end
   end
     
     
